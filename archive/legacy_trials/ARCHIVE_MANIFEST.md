# Legacy Trial Archive

The previous Trial 3–18 research line was moved out of the active workspace
on 2026-07-31 at the user's request.

This is a recoverable archive, not a permanent deletion. The workspace has no
Git repository, so archiving preserves the ability to inspect or restore prior
work without allowing legacy scripts, tests, outputs or presentations to be
mistaken for the new sector-rotation pipeline.

Archived categories:

- `code/`: old trial and selector scripts;
- `tests/`: old trial-specific tests;
- `data/`: old split manifests and trial outputs;
- `research_log/`: old pre-registrations, results and review briefs;
- `presentation/`: old presentation material;
- `misc/`: old notebook, summary and stray legacy files;
- `cache/`: Python bytecode caches present during archival.

The following remain active:

- raw daily and minute data;
- `grid_platform.py`;
- `create_sector_rotation_splits.py`;
- `sector_rotation_grid.py`;
- new platform, split and strategy tests;
- new sector-rotation split/output directories;
- new sector-rotation research plan and development result.
